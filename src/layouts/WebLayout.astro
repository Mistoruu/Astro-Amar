---
interface Props {
  language?: string;
  title?: string;
}

const { language = "en", title = "Test Website" } = Astro.props;

import WebHeader from "../components/WebHeader.astro";
import "../styles/test.scss";
---

<!-- HTML 5 --><!doctype html>
<html lang={language}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
  </head>
  <body>
    <!-- HEADER -->
    <WebHeader />
    <slot />
    <!--FOOTER -->
  </body>
</html>
